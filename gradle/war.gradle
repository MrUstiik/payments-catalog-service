apply plugin: "war"

bootWar {
    mainClassName = "com.ftr.dgb.payments.catalog.PaymentsCatalogServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
